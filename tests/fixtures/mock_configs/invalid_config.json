{
  "dag_id": "invalid_config_for_testing",
  "description": "Invalid configuration with various validation errors",
  "schedule_interval": "0 2 * * *",
  "start_date": "2024-01-01",
  "default_args": {
    "owner": "airflow",
    "retries": 1
  },
  "tasks": [
    {
      "task_id": "task_with_bad_dependency",
      "operator": "BashOperator",
      "params": {
        "bash_command": "echo 'Test'"
      },
      "dependencies": ["non_existent_task"]
    },
    {
      "task_id": "duplicate_task",
      "operator": "BashOperator",
      "params": {
        "bash_command": "echo 'First'"
      }
    },
    {
      "task_id": "duplicate_task",
      "operator": "BashOperator",
      "params": {
        "bash_command": "echo 'Second'"
      }
    }
  ]
}
